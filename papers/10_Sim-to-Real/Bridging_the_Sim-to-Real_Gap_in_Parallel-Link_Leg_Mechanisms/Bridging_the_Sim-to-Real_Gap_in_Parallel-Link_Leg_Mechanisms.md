---
layout: paper
paper_order: 10
title: "Bridging the Sim-to-Real Gap in Parallel-Link Leg Mechanisms via Simulator-Side Dynamics Normalization"
zhname: "S3N：面向并联腿机构 Sim-to-Real 的仿真侧动力学归一化"
category: "Sim-to-Real"
---

# Bridging the Sim-to-Real Gap in Parallel-Link Leg Mechanisms via Simulator-Side Dynamics Normalization
**并联腿在仿真里被简化成「串联树」后丢了执行器耦合惯量与连杆质量——本文不改真机、只在仿真侧做动力学归一化（S3N），用辨识出的硬件参数把串联仿真器补成等效并联动力学，从而缩小 sim-to-real 差距**

> 📅 阅读日期: 2026-08-17
>
> 🏷️ 板块: 10 Sim-to-Real · 并联腿机构 · 仿真侧动力学归一化 · 系统辨识 · 频响辨识
>
> 🔁 推进轨: 模块轮转（09_State_Estimation → **10_Sim-to-Real**）

---

## 📋 基本信息

| 项目 | 链接 |
|---|---|
| arXiv | [2608.01697](https://arxiv.org/abs/2608.01697) |
| HTML | [在线阅读](https://arxiv.org/html/2608.01697v2) |
| PDF | [下载](https://arxiv.org/pdf/2608.01697) |
| 源码 | 暂未开源（截至记录日未检索到官方 GitHub / 项目页） |
| **发布时间** | 2026-08-03（arXiv v1）/ 2026-08-06（v2） |
| 作者 / 机构 | Jinsong Hong、Jangho Kim、Jihwan Lee、Sehoon Oh（DGIST 机器人与机电工程系 / AI 专业，韩国大邱）、Donghyun Kim（University of Massachusetts Amherst） |

**机器人平台**：一台**并联连杆（parallel-link）腿**四足机器人（每条腿 2-DoF）。

**领域归属**：腿足机器人 **Sim-to-Real**——针对「**并联腿被建模为串联树代理**」这一常见工程简化，在**仿真侧**修正被丢弃的动力学，而不改动策略、也不改动真机硬件。

---

## 🎯 一句话总结

高性能腿足机器人常用**并联连杆机构**（闭链、多电机共同驱动一条腿），但主流 RL 仿真器（IsaacSim/MuJoCo 的树形拓扑）难以直接处理闭链，于是工程上普遍把并联腿**近似成串联树**来训练。传统做法用**雅可比坐标映射**保证运动学一致（关节角/末端位置对得上），但它**只保运动学、不保动力学**：① 执行器惯量与阻尼在并→串坐标变换后会产生**非对角耦合项**，被串联对角矩阵丢掉；② 连杆本身的**质量/惯量**在结构缩减时被直接忽略。结果即使关节状态匹配，**关节加速度、地面反作用力、机体运动仍失配**，形成 sim-to-real 差距。本文提出 **S3N（Simulator-Side System Normalization，仿真侧系统归一化）**：**不改真机、不加显式闭链约束**，而是用辨识出的硬件参数**把串联仿真器的等效动力学「补回」成并联真机的等效动力学**，同时保持树形拓扑（对现有仿真器友好）。两种变体——**S3N-Act** 补执行器级耦合、**S3N-Full** 再补连杆残余惯量。实测把关节位置/力矩误差降约 **80%**、地面反作用力误差降 **62–65%**、圆周步态的 sim-to-real 速度差距从 **17.3% 收窄到 9.9%**。

---

## 📌 英文缩写速查

| 缩写 | 全称 | 解释 |
|---|---|---|
| S3N | Simulator-Side System Normalization | 仿真侧系统归一化：在串联仿真器里补回被丢的并联动力学，保持树形拓扑 |
| S3N-Act | S3N Actuator-level | 变体一：只补执行器惯量/阻尼在串联坐标下的耦合项 |
| S3N-Full | S3N Full | 变体二：在 S3N-Act 基础上再补连杆残余惯量 |
| Serial-Tree Surrogate | 串联树代理 | 把闭链并联腿近似为无环树结构以适配主流仿真器 |
| Jacobian Mapping | 雅可比映射 | 传统并↔串坐标变换，只保运动学一致 |
| FRF | Frequency Response Function | 频响函数：通过激励-响应辨识等效惯量/阻尼 |
| GRF | Ground Reaction Force | 地面反作用力，衡量接触动力学是否匹配的关键量 |

---

## ❓ 论文要解决什么问题？

- **现象**：并联腿机构（闭链）在主流树形仿真器里难以原生建模，工程上普遍**近似成串联树**。传统雅可比映射保证运动学一致，但**动力学不一致**，导致仿真训练的策略上真机掉点。
- **根因（两处被丢的动力学）**：
  1. **执行器惯量/阻尼重分布**：并→串坐标变换会引入**非对角耦合**，串联对角矩阵表达不了；
  2. **连杆惯量缺失**：结构缩减直接把连杆自身质量/惯量抹掉。
- **痛点**：即使关节角、末端位置匹配，**关节加速度、GRF、机体运动仍失配**——差距藏在动力学层，越激烈的运动越明显。
- **本文取向**：不去改真机、也不给仿真器强加闭链约束（那会破坏树形拓扑、拖慢并行仿真），而是在**仿真侧**用辨识出的硬件参数把等效动力学**归一化**回真机水平。

---

## 🧠 方法：仿真侧把「串联」补成等效「并联」

### 1）S3N-Act：补执行器级耦合

用**动能不变（kinetic-energy-invariant）坐标变换**，把执行器惯量与阻尼从并联电机坐标「拉回」串联关节坐标，从而**显式暴露非对角耦合项**；再用辨识出的**齿轮比与电机参数**算出**补偿力矩**，把这些被串联对角矩阵丢掉的耦合项重新注入仿真。这一步**无需显式闭环约束**，仍保持树形拓扑。

### 2）S3N-Full：再补连杆残余惯量

在 S3N-Act 之上，**单独辨识整条并联腿的总惯量**（通过腿级频响 FRF），**减去已计入的执行器贡献**（避免重复计数），把剩下的**连杆残余惯量**作为**位形相关（configuration-dependent）的补偿力矩**注入仿真。于是仿真器的等效惯量矩阵在数值上逼近真机。

### 3）训练 → 部署流程（对应论文 Algorithm 1）

1. **硬件辨识**：分别做执行器级、腿级 FRF，得到齿轮比/电机参数与等效惯量；
2. **构造 S3N 补偿项**：按 S3N-Act / S3N-Full 生成补偿力矩；
3. **在归一化仿真器中训策略**（补偿后动力学 ≈ 真机）；
4. **直接部署真机**——补偿只发生在仿真侧，真机端**无需任何在线补偿**。

---

## 🧭 整体流程（mermaid）

<div class="mermaid">
flowchart TB
    subgraph HW["① 硬件辨识 (一次性)"]
        ACT["🔩 执行器级 FRF<br/>→ 齿轮比 / 电机惯量·阻尼"]
        LEG["🦿 腿级 FRF<br/>→ 整条并联腿总惯量"]
    end

    subgraph PROB["② 问题：并联腿→串联树代理"]
        PAR["⛓️ 并联连杆真机<br/>(闭链, 多电机共驱)"]
        JAC["📐 雅可比映射<br/>只保运动学一致"]
        LOSS["⚠️ 丢失动力学:<br/>①执行器耦合(非对角)<br/>②连杆惯量"]
        PAR --> JAC --> LOSS
    end

    subgraph S3N["③ S3N 仿真侧动力学归一化 (保持树形拓扑)"]
        A1["S3N-Act<br/>动能不变坐标变换<br/>→ 补执行器非对角耦合力矩"]
        A2["S3N-Full<br/>总惯量 − 执行器贡献<br/>→ 补连杆残余惯量(位形相关)"]
        A1 --> A2
    end

    ACT --> A1
    LEG --> A2
    LOSS -.被补回.-> S3N

    S3N --> SIM["🖥️ 归一化仿真器<br/>等效动力学 ≈ 真机"]
    SIM --> TRAIN["🎯 RL 训练策略 π"]
    TRAIN -->|"直接部署 · 真机端零补偿"| REAL["🤖 并联腿四足真机<br/>关节/力矩/GRF/步态更匹配"]
</div>

---

## 💡 核心贡献

1. **定位并联→串联的动力学损失**：指出雅可比映射「只保运动学」的局限，把 sim-to-real 差距明确归因于**执行器耦合惯量**与**连杆惯量**两处被丢的动力学。
2. **S3N 仿真侧归一化范式**：不改真机、不加闭链约束、**保持树形拓扑**（对主流大规模并行仿真器友好），用辨识参数把串联仿真器补成等效并联动力学。
3. **两级变体 S3N-Act / S3N-Full**：分别以坐标变换补执行器耦合、以频响辨识补连杆残余惯量，且**避免重复计数**。
4. **真机验证**：在并联腿四足上，2-DoF 动态响应、俯仰站立 GRF、圆周步态迁移三类实验全面验证，误差大幅下降。

---

## 📊 关键发现

| 实验 / 指标 | 结果 | 相对基线（雅可比映射） |
|---|---|---|
| 2-DoF 接触自由动态响应 · 关节位置 RMSE | S3N-Full | ↓ **80.9%** |
| 2-DoF 接触自由动态响应 · 关节力矩 RMSE | S3N-Full | ↓ **82.1%** |
| 俯仰站立（pitch-in-place） · GRF 范数 RMSE | S3N-Act / S3N-Full | ↓ **65.1% / 62.4%** |
| 圆周步态策略迁移 · 相位平均速度差距 | 17.3% → **9.9%** | 差距近乎减半 |

**要点**：① S3N-Act 只补执行器耦合就已显著改善 GRF 匹配；② S3N-Full 进一步补连杆惯量，在动态响应误差上收益最大；③ 策略级（圆周步态）迁移证明动力学归一化确实转化为**真实任务性能**的提升。

> 📌 数值以官方 PDF 为准；本笔记基于 arXiv 摘要 + HTML（v2）整理。

---

## 🤖 对人形 / Sim-to-Real 领域的意义

| 方向 | 含义 |
|---|---|
| **并联机构友好** | 大量高性能腿足/人形（含膝踝并联、五连杆腿）都用并联传动，S3N 让这些机构能在树形仿真器里被更真实地训练 |
| **仿真侧 vs 硬件侧** | 与「塑形硬件逼近仿真」（如 Actuator Reality Shaping）互补：本文反过来「改仿真逼近硬件」，两条路线可组合 |
| **保持并行效率** | 不引入闭链约束，保留树形拓扑与大规模并行采样吞吐，工程可落地 |
| **辨识可复用** | 硬件辨识一次性完成，之后同一平台反复训练/换策略都能复用归一化参数 |

---

## 🎤 面试参考

**Q：并联腿为什么要近似成串联树？带来什么问题？**
A：主流 RL 仿真器（IsaacSim/MuJoCo）用树形拓扑，难以原生处理闭链并联机构，工程上便把并联腿近似成串联树来训练。传统雅可比映射能保运动学一致，但丢了两处动力学：① 执行器惯量/阻尼在并→串变换后的非对角耦合项；② 连杆自身惯量。结果关节加速度、GRF、机体运动失配，形成 sim-to-real 差距。

**Q：S3N 的核心思路是什么？为什么在「仿真侧」做？**
A：S3N（仿真侧系统归一化）用辨识出的硬件参数，把串联仿真器的等效动力学补回并联真机水平，同时保持树形拓扑。选在仿真侧做是因为：不改真机、不加闭链约束（那会破坏并行仿真效率），且训练完直接部署、真机端零补偿。

**Q：S3N-Act 和 S3N-Full 的区别？**
A：S3N-Act 用动能不变坐标变换把执行器惯量/阻尼拉回串联坐标、暴露并补上非对角耦合力矩；S3N-Full 在此之上再单独辨识整条腿的总惯量，减去执行器贡献（避免重复计数）后，把连杆残余惯量作为位形相关的补偿力矩注入。前者主攻执行器耦合，后者补齐连杆质量。

**Q：效果如何量化？**
A：2-DoF 动态响应的关节位置/力矩 RMSE 降约 80%，俯仰站立 GRF 误差降 62–65%，圆周步态迁移的速度差距从 17.3% 收窄到 9.9%。

---

## 🔗 相关阅读

- [Actuator Reality Shaping for Zero-Shot Sim-to-Real (2607.02205)](https://arxiv.org/abs/2607.02205)：反方向「塑形硬件逼近仿真」，与本文「改仿真逼近硬件」正好对照，本仓库已有笔记
- [Simulator Adaptation via Proprioceptive Distribution Matching (2604.11090)](https://arxiv.org/abs/2604.11090)：同为「改仿真」路线，用本体感知分布匹配辨识仿真器，本仓库已有笔记
- [PolySim: Multi-Simulator Dynamics Randomization (2510.01708)](https://arxiv.org/abs/2510.01708)：多仿真器动力学随机化缩小 sim-to-real 差距，本仓库已有笔记
- [Sim-to-Real of Humanoid Locomotion via Joint Torque Space Perturbation Injection (2603.21853)](https://arxiv.org/abs/2603.21853)：在关节力矩空间注入扰动增强鲁棒性，同着眼执行器/力矩层，本仓库已有笔记

---

> 备注：本笔记基于 arXiv 摘要 + HTML（v2）整理；论文截至记录日未检索到官方开源仓库，故未附源码运行时序图，相关数值/实现细节以官方 PDF 为准。
